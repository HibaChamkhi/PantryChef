# Expo HAS CHANGED

This project is pinned to Expo SDK 54 because the local Xcode (26.1.1) cannot build SDK 57 native code.
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
Do not upgrade the Expo SDK without updating Xcode to 26.4 or newer first.
